"use strict" //it treats all JS code as newer version
//alert(3+3) we r using node js not browser so it is not used
console.log(3
    +
    3); 
    //this will run/correct but code readability is bad so not do
  
    let name="ayaz"
    let age=12
    let isLoggedin=false 

    //data types are:
    //number -> 2 to power 53
    //bigint (for big projects)
    //string -> ""
    //boolean -> true/false
    //null-> standalone value (not defined value)
    //undefined ->
    //symbol -> for uniqueness
    //object

    console.log(typeof"ayaz")
    console.log(typeof age) // ; lagao ya nahi no effect
    console.log(typeof undefined) //undefined
    console.log(typeof null) //object